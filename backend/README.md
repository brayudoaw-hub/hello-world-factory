# Hello World Factory - Backend

Backend API for the Hello World Factory project.

## Setup

```bash
npm install
npm start
```

## Endpoints

- `GET /hello` - Returns hello message with timestamp
- `GET /health` - Health check endpoint

## Port

Server runs on port 3001
